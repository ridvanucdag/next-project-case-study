export enum HttpMethod {
  GET = "get",
  POST = "post",
  PUT = "put",
  PATCH = "patch",
  DELETE = "delete",
}

export enum QueryOptions {
  RETRY = 0,
  RETRY_DELAY = 1000,
  expiresInMins = 60,
}

export enum ApiEndpoints {
  PRODUCTS = "products",
}

